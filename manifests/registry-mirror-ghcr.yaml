# ==============================================================================
# Docker Registry v2 - Pull-Through Cache for GHCR (GitHub Container Registry)
# ==============================================================================
# Caches ghcr.io images locally for faster pulls during CI builds.
# Used by buildkit via registry mirror configuration.
# ==============================================================================

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: registry-ghcr-config
  namespace: registry-mirror
data:
  config.yml: |
    version: 0.1
    log:
      level: info
      formatter: json
    storage:
      filesystem:
        rootdirectory: /var/lib/registry
      cache:
        blobdescriptor: inmemory
      delete:
        enabled: true
      maintenance:
        uploadpurging:
          enabled: true
          age: 720h
          interval: 24h
          dryrun: false
    http:
      addr: :5000
      headers:
        X-Content-Type-Options: [nosniff]
    proxy:
      remoteurl: https://ghcr.io
      ttl: 720h
    health:
      storagedriver:
        enabled: true
        interval: 10s
        threshold: 3

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-ghcr-data
  namespace: registry-mirror
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: csi-rbd-sc
  resources:
    requests:
      storage: 200Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: registry-mirror-ghcr
  namespace: registry-mirror
  labels:
    app.kubernetes.io/name: registry-mirror-ghcr
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: registry-mirror-ghcr
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: registry-mirror-ghcr
    spec:
      containers:
        - name: registry
          image: registry:2
          ports:
            - containerPort: 5000
              name: http
          env:
            - name: REGISTRY_STORAGE_DELETE_ENABLED
              value: "true"
          volumeMounts:
            - name: config
              mountPath: /etc/docker/registry
              readOnly: true
            - name: data
              mountPath: /var/lib/registry
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: registry-ghcr-config
        - name: data
          persistentVolumeClaim:
            claimName: registry-ghcr-data

---
apiVersion: v1
kind: Service
metadata:
  name: registry-mirror-ghcr
  namespace: registry-mirror
  labels:
    app.kubernetes.io/name: registry-mirror-ghcr
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: registry-mirror-ghcr
