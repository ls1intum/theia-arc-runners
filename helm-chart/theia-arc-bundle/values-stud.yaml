# Cluster-specific values for stud.
# Purpose: AMD64 BuildKit runner scale sets for EduIDE and ls1intum.
#
# stud runs in parallel to theia-prod during migration, so runner scale set
# names must be distinct to avoid GitHub ARC listener session conflicts.

global:
  storageClass: "csi-rbd-sc"
  nodeSelector:
    kubernetes.io/arch: amd64

createNamespaces: false


ghaArcScaleSetAmdEduide:
  enabled: true
  runnerScaleSetName: "arc-buildkit-eduide-stud-amd64"

ghaArcScaleSetAmdLs1intum:
  enabled: true
  runnerScaleSetName: "arc-buildkit-ls1intum-stud-amd64"

ghaArcScaleSetArmEduide:
  enabled: false

ghaArcScaleSetArmLs1intum:
  enabled: false
