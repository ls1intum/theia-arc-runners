# Shared defaults for all clusters. Apply exactly one cluster overlay on top:
# - values-stud.yaml
# - values-theia-prod.yaml
# - values-parma.yaml
#
# Cluster overlays select storage, architecture, and the active runner scale
# sets. Keep all scale sets disabled here so a base-only render cannot
# accidentally register production runners.

global:
  storageClass: "csi-rbd-sc"
  nodeSelector:
    kubernetes.io/arch: amd64
  tolerations: []

createNamespaces: true

ghaArcController:
  enabled: true

# AMD64 BuildKit runner set that routes docker builds to stateful BuildKit workers.
# BuildKit workers live in the buildkit namespace (raw manifests).
# Cluster overlays set the final runner label, e.g. stud or theia-prod.
ghaArcScaleSetAmdEduide:
  enabled: false
  githubConfigUrl: "https://github.com/EduIDE"
  githubConfigSecret: "github-arc-secret-eduidec"
  minRunners: 10
  maxRunners: 50
  runnerScaleSetName: "arc-buildkit-eduide-theiaprod-amd64"
  controllerServiceAccount:
    namespace: arc-systems
    name: theia-arc-systems-gha-rs-controller
  buildkit:
    namespace: buildkit
    numWorkers: 7
  template:
    spec:
      serviceAccountName: arc-runner-set-amd-sa
      initContainers:
        - name: init-dind-externals
          image: ghcr.io/actions/actions-runner:latest
          command: ["cp", "-r", "/home/runner/externals/.", "/home/runner/tmpDir/"]
          volumeMounts:
            - name: dind-externals
              mountPath: /home/runner/tmpDir
      containers:
        - name: dind
          image: docker:dind
          args:
            - dockerd
            - --host=unix:///var/run/docker.sock
            - --group=1001
            - --registry-mirror=http://131.159.88.117:30081
            - --insecure-registry=131.159.88.117:30081
          securityContext:
            privileged: true
          volumeMounts:
            - name: work
              mountPath: /home/runner/_work
            - name: dind-sock
              mountPath: /var/run
            - name: dind-externals
              mountPath: /home/runner/externals
        - name: runner
          image: ghcr.io/actions/actions-runner:latest
          command: ["/home/runner/run.sh"]
          env:
            - name: DOCKER_HOST
              value: unix:///var/run/docker.sock
            - name: ACTIONS_RUNNER_REQUIRE_JOB_CONTAINER
              value: "false"
            # Injected into every job so workflow steps can compute the correct BuildKit worker
            # address without hard-coding the namespace or replica count.
            - name: BUILDKIT_NAMESPACE
              value: buildkit
            - name: BUILDKIT_NUM_WORKERS
              value: "7"
          volumeMounts:
            - name: work
              mountPath: /home/runner/_work
            - name: dind-sock
              mountPath: /var/run
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: 4000m
              memory: 8Gi
      volumes:
        - name: work
          emptyDir: {}
        - name: dind-sock
          emptyDir: {}
        - name: dind-externals
          emptyDir: {}

# ls1intum AMD64 BuildKit runner set.
# Uses the same AMD64 BuildKit worker pool as EduIDE, but registers against a
# separate GitHub organization and secret.
ghaArcScaleSetAmdLs1intum:
  enabled: false
  githubConfigUrl: "https://github.com/ls1intum"
  githubConfigSecret: "github-arc-secret-ls1intum"
  minRunners: 10
  maxRunners: 50
  runnerScaleSetName: "arc-buildkit-ls1intum-theiaprod-amd64"
  controllerServiceAccount:
    namespace: arc-systems
    name: theia-arc-systems-gha-rs-controller
  buildkit:
    namespace: buildkit
    numWorkers: 7
  template:
    spec:
      serviceAccountName: arc-runner-set-amd-sa
      initContainers:
        - name: init-dind-externals
          image: ghcr.io/actions/actions-runner:latest
          command: ["cp", "-r", "/home/runner/externals/.", "/home/runner/tmpDir/"]
          volumeMounts:
            - name: dind-externals
              mountPath: /home/runner/tmpDir
      containers:
        - name: dind
          image: docker:dind
          args:
            - dockerd
            - --host=unix:///var/run/docker.sock
            - --group=1001
            - --registry-mirror=http://131.159.88.117:30081
            - --insecure-registry=131.159.88.117:30081
          securityContext:
            privileged: true
          volumeMounts:
            - name: work
              mountPath: /home/runner/_work
            - name: dind-sock
              mountPath: /var/run
            - name: dind-externals
              mountPath: /home/runner/externals
        - name: runner
          image: ghcr.io/actions/actions-runner:latest
          command: ["/home/runner/run.sh"]
          env:
            - name: DOCKER_HOST
              value: unix:///var/run/docker.sock
            - name: ACTIONS_RUNNER_REQUIRE_JOB_CONTAINER
              value: "false"
            - name: BUILDKIT_NAMESPACE
              value: buildkit
            - name: BUILDKIT_NUM_WORKERS
              value: "7"
          volumeMounts:
            - name: work
              mountPath: /home/runner/_work
            - name: dind-sock
              mountPath: /var/run
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: 4000m
              memory: 8Gi
      volumes:
        - name: work
          emptyDir: {}
        - name: dind-sock
          emptyDir: {}
        - name: dind-externals
          emptyDir: {}

# ARM64 BuildKit runner set for parma.
# Routes docker builds to stateful BuildKit workers in the buildkit namespace.
# Cluster overlays set the final runner label.
ghaArcScaleSetArmEduide:
  enabled: false
  githubConfigUrl: "https://github.com/EduIDE"
  githubConfigSecret: "github-arc-secret-eduidec"
  minRunners: 10
  maxRunners: 50
  runnerScaleSetName: "arc-buildkit-eduide-parma-arm64"
  controllerServiceAccount:
    namespace: arc-systems
    name: theia-arc-systems-gha-rs-controller
  buildkit:
    namespace: buildkit
    numWorkers: 7
  template:
    spec:
      serviceAccountName: arc-runner-set-arm-sa
      initContainers:
        - name: init-dind-externals
          image: ghcr.io/actions/actions-runner:latest
          command: ["cp", "-r", "/home/runner/externals/.", "/home/runner/tmpDir/"]
          volumeMounts:
            - name: dind-externals
              mountPath: /home/runner/tmpDir
      containers:
        - name: dind
          image: docker:dind
          args:
            - dockerd
            - --host=unix:///var/run/docker.sock
            - --group=1001
            - --registry-mirror=http://131.159.88.117:30081
            - --insecure-registry=131.159.88.117:30081
          securityContext:
            privileged: true
          volumeMounts:
            - name: work
              mountPath: /home/runner/_work
            - name: dind-sock
              mountPath: /var/run
            - name: dind-externals
              mountPath: /home/runner/externals
        - name: runner
          image: ghcr.io/actions/actions-runner:latest
          command: ["/home/runner/run.sh"]
          env:
            - name: DOCKER_HOST
              value: unix:///var/run/docker.sock
            - name: ACTIONS_RUNNER_REQUIRE_JOB_CONTAINER
              value: "false"
            - name: BUILDKIT_NAMESPACE
              value: buildkit
            - name: BUILDKIT_NUM_WORKERS
              value: "7"
          volumeMounts:
            - name: work
              mountPath: /home/runner/_work
            - name: dind-sock
              mountPath: /var/run
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: 4000m
              memory: 8Gi
      volumes:
        - name: work
          emptyDir:
            medium: Memory
            sizeLimit: 30Gi
        - name: dind-sock
          emptyDir: {}
        - name: dind-externals
          emptyDir: {}

# ls1intum ARM64 BuildKit runner set for parma.
ghaArcScaleSetArmLs1intum:
  enabled: false
  githubConfigUrl: "https://github.com/ls1intum"
  githubConfigSecret: "github-arc-secret-ls1intum"
  minRunners: 10
  maxRunners: 50
  runnerScaleSetName: "arc-buildkit-ls1intum-parma-arm64"
  controllerServiceAccount:
    namespace: arc-systems
    name: theia-arc-systems-gha-rs-controller
  buildkit:
    namespace: buildkit
    numWorkers: 7
  template:
    spec:
      serviceAccountName: arc-runner-set-arm-sa
      initContainers:
        - name: init-dind-externals
          image: ghcr.io/actions/actions-runner:latest
          command: ["cp", "-r", "/home/runner/externals/.", "/home/runner/tmpDir/"]
          volumeMounts:
            - name: dind-externals
              mountPath: /home/runner/tmpDir
      containers:
        - name: dind
          image: docker:dind
          args:
            - dockerd
            - --host=unix:///var/run/docker.sock
            - --group=1001
            - --registry-mirror=http://131.159.88.117:30081
            - --insecure-registry=131.159.88.117:30081
          securityContext:
            privileged: true
          volumeMounts:
            - name: work
              mountPath: /home/runner/_work
            - name: dind-sock
              mountPath: /var/run
            - name: dind-externals
              mountPath: /home/runner/externals
        - name: runner
          image: ghcr.io/actions/actions-runner:latest
          command: ["/home/runner/run.sh"]
          env:
            - name: DOCKER_HOST
              value: unix:///var/run/docker.sock
            - name: ACTIONS_RUNNER_REQUIRE_JOB_CONTAINER
              value: "false"
            - name: BUILDKIT_NAMESPACE
              value: buildkit
            - name: BUILDKIT_NUM_WORKERS
              value: "7"
          volumeMounts:
            - name: work
              mountPath: /home/runner/_work
            - name: dind-sock
              mountPath: /var/run
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: 4000m
              memory: 8Gi
      volumes:
        - name: work
          emptyDir:
            medium: Memory
            sizeLimit: 30Gi
        - name: dind-sock
          emptyDir: {}
        - name: dind-externals
          emptyDir: {}

# Zot is deployed via standalone `theia-zot` chart in `zot-system`.
