# Archived Notes

This directory contains historical migration and incident notes. These files are useful for context, but they are not the active source of truth and may mention old labels, old namespaces, old PVC sizes, or retired deployment patterns.

Use the root `README.md`, `AGENTS.md`, `docs/ARCHITECTURE_V2.md`, `infra/**`, and `helm-chart/**/values*.yaml` for the current installable state.
